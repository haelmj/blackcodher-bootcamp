import {amazonBooks} from './amazon-wishlist.js'
/* import {createWishlist} from './myAmazonBooks.js'

createWishlist() */

/* {const bookTitle = document.createElement('h2');
    bookTitleItem.textContent = amazonBooks.name;
    bookTitle.appendChild(bookTitleItem);}

    {const author = document.createElement('h3');
    authorItem.textContent = amazonBooks.author;
    author.appendChild(authorItem);}

    {const price = document.createElement('h3');
    priceItem.textContent = amazonBooks.price;
    price.appendChild(priceItem);}

    {const description = document.createElement('p');
    descriptionItem.textContent = amazonBooks.description;
    description.appendChild(descriptionItem);}

    {const cover = document.createElement(`img`);
    coverItem.textContent = amazonBooks.image;
    cover.appendChild(coverItem);} */

/* document.getElementById('#booksGoHere').append(amazonBooks) */